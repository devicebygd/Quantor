<%- include('../partials/header') %>

    <!-- Page Header -->
    <section class="page-header">
        <div class="container">
            <span class="section-badge">Xidmətlərimiz</span>
            <h1>İT <span class="text-blue">Xidmətlərimiz</span></h1>
            <p>Şirkətinizin bütün İT ehtiyaclarını peşəkar komandamızla həll edirik</p>
        </div>
    </section>

    <!-- Services -->
    <section class="section">
        <div class="container">
            <div class="services-list">
                <% services.forEach(function(service, index) { %>
                <div class="service-detail-card <%= index % 2 !== 0 ? 'reverse' : '' %>">
                    <div class="service-detail-content">
                        <div class="service-detail-number"><%= String(index + 1).padStart(2, '0') %></div>
                        <h2><%= service.title %></h2>
                        <p class="service-detail-short"><%= service.shortDesc %></p>
                        <p class="service-detail-full"><%= service.fullDesc %></p>
                        <a href="/elaqe" class="btn btn-primary btn-sm">Ətraflı Məlumat</a>
                    </div>
                    <div class="service-detail-icon">
                        <div class="service-detail-icon-inner">
                            <%- include('../partials/service-icon', { icon: service.icon }) %>
                        </div>
                    </div>
                </div>
                <% }); %>
            </div>
        </div>
    </section>

    <!-- CTA -->
    <section class="cta-section">
        <div class="container">
            <div class="cta-content">
                <h2>Xüsusi həll lazımdır?</h2>
                <p>Siyahıda olmayan bir xidmətə ehtiyacınız varsa, bizimlə əlaqə saxlayın. Fərdi yanaşma ilə sizin üçün ən uyğun həlli tapacağıq.</p>
                <a href="/elaqe" class="btn btn-white">Bizimlə Əlaqə</a>
            </div>
        </div>
    </section>

<%- include('../partials/footer') %>
